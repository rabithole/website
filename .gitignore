# Secrets — PayPal Client ID/Secret, never commit these
paypal_config.json

# Database — user accounts, password hashes, order/customer data
db/

# User-uploaded product images — runtime content, not source assets
assets/uploads/

# Python bytecode cache
__pycache__/
*.pyc

# Claude Code local settings — machine-specific, may contain local paths/commands
.claude/settings.local.json

# Personal notes, not site code
Working/

# OS/editor cruft
.DS_Store
Thumbs.db
